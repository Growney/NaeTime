<h5 class="w-100 text-center">Fastest Single Lap</h5>
<table class="table table-striped">
    <tr class="px-2">
        <th>
            #
        </th>
        <th>
            Pilot
        </th>
        <th class="text-center">
            Time
        </th>
    </tr>
    <tbody>
        @foreach (var position in Positions.OrderBy(x => x.Position))
        {
            <tr>
                <td>
                    @position.Position
                </td>
                <td>
                    @position.PilotName
                </td>
                <td>
                    @($"{TimeSpan.FromMilliseconds(position.LapMilliseconds):m\\:ss\\:fff}")
                </td>
            </tr>
        }
    </tbody>
</table>